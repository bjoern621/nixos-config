# Hardware configuration (machine-specific)
hosts/*/hardware-configuration.nix

__pycache__/

.direnv/

# nix-build output symlinks
/result
/result-*

# Written by the dev shell hook into the current directory; embeds a nix store
# path that rots on GC. Unanchored: the hook lands it wherever the shell opens.
.luarc.json

# Non-free assets
#modules/fancy-boot/darwin/logo.png
#home/modules/quickshell/config/dynamic-island/icons/
#sddm icons
