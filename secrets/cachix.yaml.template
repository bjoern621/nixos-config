# Format reference for secrets/cachix.yaml, which is the encrypted file the
# cachix-push module reads. This template is read by nothing.
#
# Edit the existing secret:  sops secrets/cachix.yaml
# Create it from scratch:    cp secrets/cachix.yaml.template secrets/cachix.yaml
#                            sops -e -i secrets/cachix.yaml
#                            sops secrets/cachix.yaml   (paste the real token)

cachix-token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.PLACEHOLDER_PAYLOAD.PLACEHOLDER_SIGNATURE
