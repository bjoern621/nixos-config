-- https://wiki.hypr.land/Configuring/Variables/#debug
-- Built-in per-monitor FPS + frame-timing overlay.
-- Debug only; draws every frame.
hl.config({
    debug = {
        overlay = true,
    },
})
